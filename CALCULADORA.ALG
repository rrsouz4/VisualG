Algoritmo "CALCULADORA"

Var

   x, i, total : inteiro

Inicio

    escreval("Digite o número para qual deseja a tabuada: ")
    leia(x)
    
    para i de 1 ate 10 faca
    
    total <- x*i
    escreval(x," x",i," = ",total)
    
    fimpara

Fimalgoritmo